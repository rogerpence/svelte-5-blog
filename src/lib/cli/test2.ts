import * as utes from './helpers'
import path from "path";
import * as pagefind from "pagefind";

console.log(utes.rootDir)

const devIndexPath = path.join(utes.rootDir, 'public/pagefind')
console.log(devIndexPath)

